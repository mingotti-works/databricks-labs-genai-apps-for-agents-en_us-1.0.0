env:
  - name: MLFLOW_EXPERIMENT_ID
    valueFrom: app-experiment            # experiment app resource key
  - name: SERVING_ENDPOINT_NAME
    valueFrom: serving-endpoint          # already correct
    
command: ["uv", "run", "start-server"]
